{
  "name": "ens-explorer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service doomling/ens-explorer",
    "create-local": "graph create --node http://localhost:8020/ ens-explorer",
    "remove-local": "graph remove --node http://localhost:8020/ ens-explorer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ens-explorer",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.6",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "@graphprotocol/client-cli": "^2.2.16",
    "matchstick-as": "0.5.0"
  }
}
